<!DOCTYPE html>
<html>
<head>
    <title>ATS-CLH Report Preparation</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            background-color: #f8f9fa;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .btn-primary {
            width: 100%;
        }
        .result {
            margin-top: 20px;
            padding: 10px;
            border-radius: 5px;
        }
        .alert-success {
            background-color: #d4edda;
            color: #155724;
            border-color: #c3e6cb;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">ATS-CLH Report Preparation</h1>
        <form action="/process" method="POST">
            <div class="form-group">
                <label for="from_date">From Date:</label>
                <input type="date" class="form-control" id="from_date" name="from_date" required>
            </div>
            <div class="form-group">
                <label for="to_date">To Date:</label>
                <input type="date" class="form-control" id="to_date" name="to_date" required>
            </div>
            <div class="form-group">
                <label for="email">Amazon Email-ID:</label>
                <input type="email" class="form-control" id="email" name="email" required>
            </div>
            <button type="submit" class="btn btn-primary">Run</button>
        </form>

        <!-- Display the results -->
        {% if results %}
        <div class="result alert alert-success" role="alert">
            <h5 class="mb-3">Report Generated!</h5>
            <p>The reports have been generated successfully. Click below to download:</p>
            {% for result in results %}
            <a href="{{ result }}" class="btn btn-success btn-sm" download>Download Report {{ loop.index }}</a>
            <br>
            {% endfor %}
        </div>
        {% endif %}
    </div>
</body>
</html>
